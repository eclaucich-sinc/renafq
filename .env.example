# Copiar este archivo a ".env" (no commitear) y completar con credenciales reales.
# Usado por docker-compose.yml para levantar el contenedor de MongoDB local.
MONGO_ROOT_USERNAME=
MONGO_ROOT_PASSWORD=
